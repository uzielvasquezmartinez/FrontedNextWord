/*import api from "./api";

const userService = {
  // GET /api/users
  // Response: List<User>
  getAll: () =>
    api.get("/users"),

  // POST /api/users
  // Body: User
  create: (data) =>
    api.post("/users", data),

  // PUT /api/users/profile
  // Body: UserUpdateDto { fullName, phoneNumber, profilePicture, newPassword }
  // Requiere JWT — el backend identifica al usuario por el token
  updateProfile: (data) =>
    api.put("/users/profile", data),
};

export default userService;*/